{
  "name": "lmn",
  "version": "0.2.0",
  "description": "",
  "main": "src/main.mjs",
  "scripts": {
    "boot": "node --experimental-modules src/main",
    "lint": "eslint '{src,specs}/**/*.{js,mjs}'",
    "prettify": "prettier --write '{src,specs}/**/*.{js,mjs}'",
    "start": "nodemon --exec 'npm run prettify --silent && npm run lint --silent && npm test --silent && npm run boot || exit 1'",
    "test": "node --experimental-modules specs/main.spec"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/slacktracer/lmn.git"
  },
  "author": "slacktracer",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/slacktracer/lmn/issues"
  },
  "homepage": "https://github.com/slacktracer/lmn#readme",
  "dependencies": {
    "parse5": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^4.18.2",
    "nodemon": "^1.17.1",
    "prettier": "^1.11.1"
  }
}
